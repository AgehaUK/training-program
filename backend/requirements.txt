# Web framework
fastapi==0.128.5
uvicorn==0.39.0
starlette==0.49.3
python-multipart

# Validation / settings
pydantic==2.12.5
pydantic-settings

# Database
SQLAlchemy
psycopg2-binary

# Auth
python-jose[cryptography]
passlib[bcrypt]

# Vector search / numerics
numpy

# LLM (used only when ANTHROPIC_API_KEY is set; otherwise falls back to mock)
anthropic

# Tests
pytest
httpx
